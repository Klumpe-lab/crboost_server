"""Rendering-side helpers (previews, cutouts, IMOD/ArtiaX bridges, tomogram geometry).

The particle modules (coords, pick_merge, picks_filter, list_extraction, subtomo_link)
live in ``services/particles/``.
"""
