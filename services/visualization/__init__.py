"""Rendering-side helpers (previews, cutouts, IMOD/ArtiaX bridges, tomogram geometry).

Roadmap 09 moved the load-bearing particle modules — coords, pick_merge, picks_filter,
list_extraction, subtomo_link — to ``services/particles/``.
"""
