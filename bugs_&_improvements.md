- can start the running pipeline twice
- acquisition params should be autoinferred when user specifes mdocs (not have to click)

x denoise fix --> 
x  refactors (ui, model typing) --> 
qsub integration --> 
png display --> 
make sure things make sense --> 
"analytics"/global parameter display