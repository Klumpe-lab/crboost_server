# Sven:

- denovo particle picking (fork off from reconstruction)
https://github.com/orgs/Klumpe-lab/projects/2/views/1?pane=issue&itemId=197248700&issue=Klumpe-lab%7Ccrboost_server%7C77
- reconstruction in journey... (make linkable)

# Lorenz:
-  tilt readouts in journey
-  outfit tilt thumbnails with the associated data

# General cleanup:

- tilt filter page generalize
- cleanup the side UI (project data, switching between projects) 
- fix the fucking file browsers across the board (standardize)

# v0.1 handoff preparation

- kill off the ticks in logs :
```
14:55:37 [INFO   ] backend: Found 26 valid projects.
14:55:52 [INFO   ] backend: Scanning directory: /groups/klumpe/crboost_data
14:55:52 [INFO   ] backend: Skipped 'Michael_test' - missing project_params.json
14:55:52 [INFO   ] backend: Found 26 valid projects.
14:56:07 [INFO   ] backend: Scanning directory: /groups/klumpe/crboost_data
14:56:07 [INFO   ] backend: Skipped 'Michael_test' - missing project_params.json
14:56:07 [INFO   ] backend: Found 26 valid projects.
```
- standardize container placement on our side
- preflight checks
- sample dataset

---

# Smaller things
- fix the logs (bakcend scanning)
- sometimes when i rerun the job with a bunch of previously failed tasks and they succeed the red (!) failed task number indicator on the job row persists, which is annoying and confusing. ![alt text](image.png)


Ok let me give feedback on each cluster of issues, these would indeed be quite good to fix and would make for great consolidation. Should be done with utmost caution however, so let's build a roadmap for each and i'll tackle them separately. Put them in
  docs/roadmaps. Now to the actual feeback:
  
- 1. lets think a bit more about this. i would of course like to centralize all the central services on the backned,  but Is there any downside to having them all there? do we constrian ourselves down the line? Regarding the
  inversions -- is that easy and harmless to undo or a loadbearing piece? I honestly can't quite remember what's going on there and whether it's necessary or not. I agree completely that all the side-processing and logic that has to do with data visualization and
  quality controls (like dashboards, results extraction and processing, pixles check etc.) should be modular and extracted, have their own place to live and category -- here i agree wholeheartedly. 
- 2. Yeah, sure. Long and arduous road here with keeping track of how a
  titl series travels through the pipeline and accumulates features. Lots of lossy refactors. The last one and probabluy the only intentioanl one was indeed the registry and if we can we should indeed consolidate on it. Let's be extremely careful and not lose anything from other mechanisms that do it their own way now. All the regex shit should go away except where strictly necessary. Let's opt for robust and predicatable mechanisms.
- 3. Yeah sure. Erorr and failure reporting and loggign was always an afterhtought. If you see a good idiom for how to organize it here that would be awesome. I don't want anything too verbose and industry-grade (anywhere across this systme to be honest) because at one point we will have to rewrite it anywa, but if we can centralize and standardize the mehcnaism we already have already and give more clear and lucid/trackalbe-to-the-file logs/errors -- that'd be awesome.
- 4. Be extremely careful with the drivers. If something can be abstracted/factored out/improved towrads more lucidity/modularity -- that's great, but functional changes i'd be extrmeely cautious with becuase right now things seem to wrok, ugly as they are. If you see a clear refactor architecutre that's strictly superior -- i'm down.
-  5. Not sure how this would work, not familiar with the more recent Result developments in python, but yeah sounnds quite useful. Let's just make sure that we don't constrain our system too mcuh with this since this is still in very early stages of development and things are likely to change and hence the shapes of the functions that produces them and thier success/failure conditions...
-6. Yeha, i would very much like to avail ourselves of modern python utilities here to make the code acorss the 5 points above less shitty and more idiomatic, decoupled and robust. However that probably in many cases presupposes an irreconcilable change in the structure of the code. So i feel like introducing these new python facilities should be done at certain independent areas of the codebase. Luckily we kind of partitioned the refactor into such areas just now so i would like you for each roadmap when you are constructing it -- provide recommendations about how the new modern python language utilities coudl be woven into the refactor.

Perhaps we should start with the deletions sicne you say it is free.

Every roadmap should come with a "before and after" section that states conceptually what is to be changed and what is perceived gain from the refactor, then provide concrete technical guidelines for what to change, what to deprecate and how to do it in commitable stages. If preceise infomration is lacking about the codebase of the tools we use and their parameters -- it should be our priority to gather the necessary details first intstead of starting headlong into the changes.

-----------