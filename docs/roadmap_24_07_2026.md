# Sven:

- denovo particle picking (fork off from reconstruction)
https://github.com/orgs/Klumpe-lab/projects/2/views/1?pane=issue&itemId=197248700&issue=Klumpe-lab%7Ccrboost_server%7C77
- reconstruction in journey... (make linkable)

# Lorenz:
-  tilt readouts in journey
-  outfit tilt thumbnails with the associated data

# Michael:

- field for the gain reference in the initial setup too

# Gaia:

- skip/mute per TS

# General cleanup:

- tilt filter page generalize
- cleanup the side UI (project data, switching between projects) 
- fix the fucking file browsers across the board (standardize)

# v0.1 handoff preparation

- kill off the ticks in logs :
```
14:55:37 [INFO   ] backend: Found 26 valid projects.
14:55:52 [INFO   ] backend: Scanning directory: /groups/klumpe/crboost_data
14:55:52 [INFO   ] backend: Skipped 'Michael_test' - missing project_params.json
14:55:52 [INFO   ] backend: Found 26 valid projects.
14:56:07 [INFO   ] backend: Scanning directory: /groups/klumpe/crboost_data
14:56:07 [INFO   ] backend: Skipped 'Michael_test' - missing project_params.json
14:56:07 [INFO   ] backend: Found 26 valid projects.
```
- standardize container placement on our side
- preflight checks
- sample dataset

------

Ok we would like to prettify our app here a bit and make it more coherent, fix a few bugs and add a few features before we shipit off to our friends in munich.

Let's start with the easy things:
- one guy asked for a gain reference field to be added at setup. I'm not sure if this should be done per dataset, per project, per tilt series or whatever -- let's add it wherever appropraite while reusing our existing file browser infrastructure. Also let's make sure it gets hooked up automatically to all the jobs that require it with an appropriate indications for the user. Rather, i'm not sure how many jobs even use the gain references -- but perhaps we should have a dedicate input slot in there for them that gets autopopulated if the user has provided one at setup and if not -- allows to add the gain reference at job registration time..

- next, let's try to attack a few UI peeves of mine:
    - In the sidebar of the job regsitrations (jobtypes) we have a bunch of redundant fucking buttons on each row   (config,logs,files) and then also the "Toggle ts list" and + for add and cross for delete. Please just remove the three fucking icons (config, logs ,files) because the user can navigate them via the top bar anyway from the given job.
    - Furthermore there is this chevron underneath array jobs. It's confusing as fuck because it's there as soon as the job is added (yellow) but nothing has ran so far. It starts to get populated when the job is already running or did complete, but there is no sense in displaying it before. In fact the chevron underneath breask the flow of that interface so let's get rid of it completely and just have a "toggle tilt list chevron " sitting on the job row itself that would otglge the tasks of the array jobs as it does now. I think that would look better.
    - next, let's rework the config tab's section majorly. They are functional right now but are ugly as fuck. In particular let's reduce the number of the empty space between the parameters in the config. The style of the dropdowns is absolutely ugly material ui looking shit -- let's make it look more like the rest of the interface (take inspiration form journey or the job sidebar for example). Next -- same problems of lack of readability and style in the IO tab: the dropdowns options are ugly as fuck and look like some generic 2010s material ui shit -- figure out a better style to be used here for both sections. next, the dropdown frequently gets misaligned horizontally between the consecutive vertical input slots because of the thing listed after the slot: ex. for the recon job the filtered_tilt_series_star.star (+1) and warp_tiltseries_dir are different length so options dropdowns is staggered. It looks ugly and messy. This panel can also be a lot more informative or more concisely communicate the information the pipeline already possesse and explain itself more. For example when the job is quued and its inputs are also only queued -- the option says something like "[scheduled] scheduled  -- TS CTF" . It's just ugly and im not sure why it must be spelled twice. We can also make it more obvious that the input slot refers to the output of the previous job by styling the options here as the job rows in the sidebar with the corresponding status dots. Why not do that? Importantly, right now if i have 2 alignment jobs sitting queued in the pipeline -- both of their "Input Processing" display as [scheduled] scheduled Alignment so there is no fuckign way to differentiate them. Can we please at least include the full name there? It's completely useless otherwise. The "Manual path" of each option does create a path underneath the input slot to be filled in, but its file browser when clicked just says "File picker not yet implemented". Annoying, please implement this. 
    - Lastly for the IO panel -- let's separate the Input Slots from Output Slots via a LITTLE BIT of vertical space and small titles. Also for each slot (both input and output) let's standardize how they are displayed because right now the input slots have a slightly blacker name first (eg. Input Star) and thne the grayed out expansion to something like "filtered_tilt_series_star.star (+1)" -- which it's not fucking clear at all what that means and only then the dropdown with optiuon comes from. But output slots only have the name and it is grayed out and the the grayed out expansion. It's super fucking confusing. Please just keep the name in the same font (Input Star or Output Star) but add a bigger on hover panel with everything the user might want to know to understand this line better -- in the case of input slot: what the fuck the given artifact type means (what the fuck is even warp_dir, which one this job prefers over others if we have any special rules there etc.). For the output slot -- that the full path where the output will be place is "predicted/computed" but will only exist after the job is done. Display full abs path in this case.


----

Ok i just moved bunch of containers that our application here uses (via config.yaml) to our groups folder so its all centralized. They were quite scattered currently, but im obviously quite afraid to mess up the paths. I want you to verify that these containers indeed exist after i just moved them. 

Also let us rework two in-application menus s.t. the user is also capable of doing this without necessarily mucking about with the paths manually and editing the config itself as a text file. 
- let's add a thin strip of ui saying something like "CryoBoost" on the landing page above the project setup and projects overivew. It should contain a settings icon (that when clicked opens our config file as nicely formatted editable file that the user can override in place) and a bunch of status dots for the important connections and info about the current instance of the server: whether its able to connect to slurm, the current user, the current port its running on, the collected check on whether all the containers exist (green/orange status dot) etc... whatever else you see as possibly informative let's stick here. Now regarding the edititng of the config in place -- we have to implement this pretty carefully because we don't one user to completely fuck up the config for everyone else so let's have an "revert to defaults" mechanism first and foremost, then the edits that are first made by the user go into their home folder (i dont rememberi f we already cache some shit by defualt per user, if we do -- reuse the same dir.. was it something like ~/.crboost..? verify) and is now preferred while it exists, whole for all other users the default stays the manually-accessible-only server-dri conf.yaml. Hopefully you see what i mean. Ottherwise this shoul just be a nicely formatted config/settings section that are already quite cleanly conceptually separate: the venv location, the per-job slurm defaults, the container defaults. Wherever a software tool or a container's path is being provided the user should get instant feedback if the file does not exist.


Now that's for the main landing page. Now let's also take care of the menus and their look inside the proejct page..there are basically two things that the user can click there at the top left of the page: the circle of the project thumbanil that call up an overview of all projects which is quite nice on its own except (FIX) the base location at the top gets cut off at like 80 pixels or something for no reason it all (the whole of the horizontal line strip is available to it). And the second thing being the project overview with shit like project name, acquisistion params, dataset, tilt series selected and slurm defaults. Lets remove the slurm defaults from there and then merge these two UIs into one like so: there is now just the project thumbnail that when clicked recalls the stitched-together panel where on the irght there is still the project overview rows but on the left there is no the project params panel for each selected project. Obviosuly make this now-joint panel much wider (it can fill up almost the whole screen) and let's change the click behavior to the following: when a user single-clicks another project row except the currently active now we don't travel striaght into it anymore but instead the left project params etc. sidebar switches to display the info for _that_ project and that row gets a faint outline so the user can have a high-levle ooverview of projects before they travel there. To travel to the project just add some obvious arrow on the proejct row.


----


