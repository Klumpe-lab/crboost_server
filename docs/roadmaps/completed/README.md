# Completed roadmaps

Roadmaps whose stages are all code-complete, plus plans that were superseded (marked as such). They
stay here as the record of what was done and why — the stage logs inside each file are the reason to
keep them. Live work is in [../README.md](../README.md).

**Most of this has not been exercised at runtime.** Code-complete means reviewed and statically
green, not verified in the app; the consolidated script is [../roadmap_arc-runtime-checklist.md](../roadmap_arc-runtime-checklist.md).

| # | Roadmap | Theme | Status |
|---|---|---|---|
| 00 | [deletions-and-lint](roadmap_00-deletions-and-lint.md) | Delete dead code, tighten ruff, fix 3 tiny bugs | **complete** — all 5 stages landed + committed 2026-08-11 |
| 01 | [service-boundary](roadmap_01-service-boundary.md) | Facade becomes real; QC/visualization processing gets its own home; undo the services→ui inversion | **complete** — all 8 stages done 2026-08-11 (`check_boundaries.py` enforces; runtime verification of stages 5–7 owed) |
| 02 | [tiltseries-single-source](roadmap_02-tiltseries-single-source.md) | Consolidate on the TiltSeriesRegistry; kill regex re-derivation; typed InstanceId | **stages 0–6 done** — 0–5 committed + runtime-confirmed 2026-08-12, stage 6 (preimport fold + TomogramGeometry rename) code-complete same day; fresh-run parity harness still owed |
| 03 | [errors-and-results](roadmap_03-errors-and-results.md) | One lightweight result/error idiom + lucid logging | **code-complete** — all 5 stages done 2026-08-12 (`ok()`/`err()` everywhere, census in `roadmap_03-stage0-census.md`, swallow triage, `load_warnings`); runtime failure-path checks owed |
| 04 | [driver-consolidation](roadmap_04-driver-consolidation.md) | ArrayDriver template, ToolCommand, one status protocol | **stages 0–2 done + 4–5 done** 2026-08-13 — census + `TaskStatusStore` + all 14 `ToolCommand`/`run_tool` migrations, the 5 transcript-covered types byte-verified on a live run ([design](roadmap_04-stage2-toolcommand-design.md), [transcripts](roadmap_04-stage2-transcripts.md)); `BaseIngestAdapter` + `subtomo_merge`→`services/` + single-sourced driver invocation. Stage 3 **COMPLETE (code) 2026-08-14** — all 8 array drivers on `ArrayDriver`/`DriverContext` (runtime owed on the 6 new ones, incl. the ts_alignment #38/#39 registry-authority pilot). Stage-4 ASK **decided 2026-08-14**: registry-stamp failure now FAILS denoise_predict + tilt_filter (landed). `extract_pick_list` identity → [roadmap 07](roadmap_07-extract-pick-list-job-identity.md) |
| 07 | [extract-pick-list-job-identity](roadmap_07-extract-pick-list-job-identity.md) | Per-list extraction becomes a real, UI-visible job (census #68); closes #73 + roadmap-04 stage-5 leftover | **CODE-COMPLETE** — S0–S4 plus an S5 review-follow-up stage that closed the 8 open findings (`reconcile_pick_list_extractions` asks SLURM instead of trusting a stored status, `cancel_pick_list_extraction` on delete, logs-dialog fixes, rmtree off the loop). PENDING RUNTIME |
| — | [denovo_picking/](denovo_picking/roadmap_00-overview.md) | De-novo species + manual picking; tomo-import hardening; project-type dissolution; aggregation factor-out | **spine S1→S2→S3 code-complete** 2026-08-13 (species creation + registry hygiene; `TomoGeometry` provider + Particles-panel inversion + ArtiaX decoupling; candidate-free extraction + resolver injection + the `384/1.0/224` default killed) — **runtime owed on all three**, no code left on the spine. S4 was absorbed by 11-S3. **S5 + S6 CODE-COMPLETE 2026-08-18, PENDING RUNTIME** — S5 tomogram-import hardening (`JobType.IMPORTED_TOMOGRAMS` ends the MERGED_SOURCES masquerade + gives a dangling imported star its own message; multi-import BATCHES rebuilt into one star with an explicit rename/skip report; `.rec` accepted; chunked probing + capped preview), S6 aggregation factor-out (`services/aggregation/` + `ui/aggregation/`; `_MergeDialog` kills the cross-tab module globals; SingleFlight; GateReport pre-flight; **`is_aggregation` deleted** — merging is now a PARTICLES-header capability of every project, and project creation has no type switches left). D-8 amended by S3 (producer id is species+tomo+slug scoped); supersedes `notes/PARTICLE_PROJECT_ROADMAP.md` |
| 08 | [species-reactive-spine](roadmap_08-species-reactive-spine.md) | `registry_rev` + `species_identity()` in every refresh gate; species persistence fix; workbench refresh-on-show; "Template Extract" → "Pick candidates"; one species pill + job-tab slimming | **S0 + S1 code-complete** 2026-08-16 (PENDING RUNTIME — checklist in the doc §6); scoped same day (root cause of the "de-novo species not in workbench" bug + persistence hazard) — FIRST slice of the species-registry arc |
| 09 | [particles-services-and-watcher](roadmap_09-particles-services-and-watcher.md) | `services/particles/` move; ingest service; `species_overview` reader; PickList provenance fields + `catalog_id` hook; server-side curation watcher (ArtiaX `.coords` detection off the Journey render path) | **S1–S4 code-complete** 2026-08-16 (`services/particles/` move · ingest service + provenance fields · `species_overview` reader · `CurationWatcher` replaces the Journey prescan) — PENDING RUNTIME (`py_compile` + `check_boundaries` + §4 checklist owed); scoped same day |
| 10 | [species-page](roadmap_10-species-page.md) | "Species" page replaces the Template Workbench view: rail + compact tabs (Overview · Templates & masks [mounted workbench] · Picks · Curation · Jobs); identity editor + delete cascade → service; Jobs tab with drift chips | **CODE-COMPLETE** 2026-08-16 — S1+S2 (one commit: shell + rail + segmented tabs + `create_species` + mounted workbench tab), S3 (Overview = identity editor (P-02 closed) + status/sanity block + delete → `services/species_admin`), S4 (Jobs tab + `services/particles/species_jobs`, `open_job` / `add_instance_for_species` hooks); PENDING RUNTIME (checklist in the doc) |
| 11 | [picks-actions-consolidation](roadmap_11-picks-actions-consolidation.md) | `ListRef` + `list_actions` carve; Picks tab (cross-tomo table, extract-all-pending via GateReport); Journey declutter (Journey = look & curate + ⚡; Species = manage & act); Curation tab v1 | **ALL 4 STAGES CODE-COMPLETE** — S1 2026-08-16 (`services/particles/list_ref.py` + `ui/particles/list_actions.py`, Journey repointed, `deduplicate_pick_list` list-keyed), S2 2026-08-17 (`ui/species/picks_tab.py`: cross-tomo table on `species_overview`, per-row extract / dedup / delete, per-tomo merge bar + auth radio, "Extract all pending" behind the gate-report pre-flight, `journey_show_ts` hook, `list_actions.dialog_host()`), S4 2026-08-17 (`ui/species/curation_tab.py` + shared `ui/particles/session_status.py` with a 3-state live/off/**unknown** poll; watcher log + unattributed-saves block; `species_tomo_map`/`auto_ref` shared by both tabs). S3 2026-08-17 (Journey declutter: merge ticks + inline merge bar + `_MERGE_SELECT`, extraction bar, clash/dedup panel, toolbox Curate + Import all removed; auth radio display-only; ⚡ falls back to `curate_in_artiax` with no live session; NEW "manage in Species ↗" route; de-novo strip `subtomo_status` derived — `dashboard_data.pick_list_subtomo_status`, two-state not three, see its stage record for why `warn` was dropped; 6010 → 5861 lines, 5854 after folding in two `ruff format` reflows of its own edits). **ALL FOUR STAGES CODE-COMPLETE, ALL PENDING RUNTIME** — verify in one pass with the arc. S3's one-session hold was lifted by the maintainer 2026-08-17, so it landed on code-review confidence (preflight audit + 4-dimension adversarial review; 4 distinct defects caught and fixed, and a list of pre-existing items reported-not-fixed in the stage record). Live extraction status still gated on 07 |
| 12 | [species-catalog](roadmap_12-species-catalog.md) | lab-level catalog of species definitions (two-tier model; picks stay per project); aggregation groups by `catalog_id` | **ALL 4 STAGES CODE-COMPLETE 2026-08-18** — S1 `species_catalog_root` config (empty = feature off), S2 `services/particles/catalog.py` (immutable `v<N>` version dirs, `extra="forbid"`, sidecars travel so template UUIDs stay stable), S3 rail "From catalog" + Overview "Publish to catalog" (`ui/species/catalog.py`, schema 3.5 adds `catalog_version`), S4 `SubtomoCandidate.catalog_id` + the merge card's `⌗` chip that selects every project holding that species. PENDING RUNTIME |
| — | [picking_ui/01–06](picking_ui/) | the 2026-08-18 walkthrough's Particles/Species UI pass: species creation form + nav icon (01) · species-derived fields in job tabs (02) · Overview identity + bindings (03) · Overview declutter + extraction-geometry panel (04) · Templates & masks in two columns with table rows (05) · one font scale in the workbench forms (06) | **all six code-complete 2026-08-18**, pending runtime. The index and the guideline of record: [picking_ui/roadmap_00-overview.md](picking_ui/roadmap_00-overview.md); the deferred cross-project registry scoping stays live at [../picking_ui/roadmap_07-scoping-particle-registry-edges.md](../picking_ui/roadmap_07-scoping-particle-registry-edges.md) |
| — | picking_ui/[00](picking_ui/roadmap_00-overview.md) · [08](picking_ui/roadmap_08-house-controls-and-workbench-chrome.md) · [09](picking_ui/roadmap_09-picks-curation-one-surface.md) · [11](picking_ui/roadmap_11-pick-viewer.md) · [12](picking_ui/roadmap_12-aggregate-candidates.md) · [13](picking_ui/roadmap_13-seeded-list-and-session-switch.md) | wave 2 + 3: control vocabulary + workbench chrome (08), Picks & curation one surface (09), pick viewer (11), aggregate candidates (12), pre-seeded ArtiaX list + in-session switch (13) | code-complete, pending one runtime pass. **Files deleted in `bc2b41b` — restore pending** |
| 14 | [protocols-regression-harness](roadmap_14-protocols-regression-harness.md) | Protocols (schema · export · apply) + a regression harness | **superseded by 16** 2026-09-04 — protocol core kept, harness deleted |
| 16 | [protocols-workbench](roadmap_16-protocols-workbench.md) | Create a regular project from a protocol; workspace Protocols view; `ProjectState.protocol_origin` | code-complete; runtime §6 owed |
| 17 | [url-routing](roadmap_17-url-routing.md) | Addressable URLs `/p/<project>/<view>/<target>`, History API only | S0–S5 code-complete 2026-09-08; runtime §7 owed |
| 18 | [k3-stack-ingest](roadmap_18-k3-stack-ingest.md) | SerialEM/K3 tilt stacks from the landing page; dose estimated + flagged | S0–S4 code-complete 2026-09-09; runtime §6 owed |
| — | [artiax-bridge](roadmap_artiax-bridge.md) | ChimeraX + ArtiaX manual-picking bridge over VNC (append-only session history) | bridge landed (`services/visualization/artiax_bridge.py`); its UI sections **superseded** by 11 and picking_ui/09, 10, 13 |
| — | [list-extraction-and-aggregation](roadmap_list-extraction-and-aggregation.md) | Per-list extraction + cross-project aggregation design | **superseded** — authoritative-list model deleted (schema 3.6); replaced by picking_ui/12 |
| — | [picks-filter-aggregation](roadmap_picks-filter-aggregation.md) | Picks filter + merged-optset aggregation | ① landed (`active_merged_optset`); **rest superseded** by denovo S6 (`services/aggregation/`) and picking_ui/12 |
| — | [scheduler-proposal](roadmap_scheduler-proposal.md) | In-process scheduler loop to replace `relion_schemer` | **superseded** by [../roadmap_orchestrator-replacement.md](../roadmap_orchestrator-replacement.md) (SLURM afterok DAG) |
